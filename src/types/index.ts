
/**
 * vision - The model can process and understand images.
 * search - The model can perform search operations.
 * file - The model can handle file uploads and processing.
 * reasoning - The model can perform reasoning tasks.
 * image - The model can generate or manipulate images.
 */
export type ModelFeature = "vision" | "search" | "file" | "reasoning" | "image";